{
  "name": "neon-launch-vercel-example",
  "version": "0.0.0",
  "private": true,
  "description": "Reference config for `neon launch` against a Next.js + Drizzle + Vercel + Neon Postgres stack.",
  "scripts": {
    "launch": "neon launch",
    "launch:preview": "neon launch --preview",
    "launch:prod": "neon launch --prod"
  },
  "devDependencies": {
    "neonctl": "*"
  }
}
